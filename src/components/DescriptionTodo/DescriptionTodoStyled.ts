import styled from '@emotion/styled';

export const DescriptionTodoWrapper = styled.p`
  font-weight: 500;
  margin-bottom: 0.5rem;
  padding-left: 0.25rem;
  padding-right: 0.25rem;
  text-overflow: ellipsis;
  white-space: nowrap;
  overflow: hidden;
`;
