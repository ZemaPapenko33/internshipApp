import styled from '@emotion/styled';

export const ProjectNameWrapper = styled.p`
  font-size: 1.125rem;
  line-height: 1.75rem;
  width: 50%;
  text-overflow: ellipsis;
  white-space: nowrap;
  overflow: hidden;
`;
