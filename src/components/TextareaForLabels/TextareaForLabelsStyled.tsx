import styled from '@emotion/styled';

export const TextAreaForLabelsWrapper = styled.textarea`
  border-bottom-width: 2px;
  border-radius: 0.25rem;
  margin-bottom: 0.5rem;
  overflow-wrap: break-word;
  padding: 0.5rem;
`;
