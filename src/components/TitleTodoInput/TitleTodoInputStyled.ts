import styled from '@emotion/styled';

export const TitleTodoInputWrapper = styled.input`
  height: 30px;
  border-width: 2px;
  border-radius: 0.25rem;
  padding: 0.5rem;
  margin-bottom: 0.5rem;
  box-shadow:
    0 10px 15px -3px rgb(0 0 0 / 0.1),
    0 4px 6px -4px rgb(0 0 0 / 0.1);
`;
