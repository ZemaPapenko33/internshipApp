import styled from '@emotion/styled';

export const SelectTodoWrapper = styled.select`
  border-radius: 0.25rem;
  border-width: 2px;
  margin-bottom: 1rem;
  box-shadow:
    0 10px 15px -3px rgb(0 0 0 / 0.1),
    0 4px 6px -4px rgb(0 0 0 / 0.1);
`;
